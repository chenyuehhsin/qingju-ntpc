# Policy interpretation

既有政策矩陣使用可审查的中位數條件。M3 僅新增高青年人口且低每千青年求才數的條件組合，沿用既有 eligible cohort 與 high >= median、low < median；門檻由 deterministic code 取得。這是觀察，非因果、政策優先排序或正式處方。

依據：`app/components/policy_lens.py`、`app/policy_agent/policy_rules.json`。
