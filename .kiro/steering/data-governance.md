# Data governance

Follow root AGENTS.md. Raw data, historical snapshots and provenance hashes are
immutable. Do not modify population/job values, source periods or geographic
coverage. Use existing processed data and preserve missing values. Keep KB
documents separate from numeric tables. Generated document hashes are new KB
integrity metadata and must not replace original source hashes.
